<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.wang</groupId>
  <artifactId>BackendLab</artifactId>
  <packaging>pom</packaging>
  <modules>
    <module>redis-lab</module>
    <module>springcloud-lab</module>
    <module>jvm-lab</module>
    <module>rabbitmq-lab</module>
    <module>kafka-lab</module>
    <module>mysql-lab</module>
    <module>netty-lab</module>
    <module>interview-notes</module>
  </modules>
  <version>1.0-SNAPSHOT</version>
  <name>BackendLab — Backend Technology Laboratory</name>
  <description>A long-term personal backend technology experiment repository for Java, JVM, middleware, Spring Cloud, MySQL, Netty, Docker, and interview topics.</description>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Shared version properties -->
    <!-- Spring Boot 3.5.3: latest stable 3.5.x, supports JDK 17-25 -->
    <spring-boot.version>3.5.3</spring-boot.version>
    <!-- Lombok 1.18.38: latest stable, supports JDK 25 -->
    <lombok.version>1.18.38</lombok.version>
    <!-- Redisson 3.50.0: latest stable, supports Spring Boot 3.x and JDK 25 -->
    <redisson.version>3.50.0</redisson.version>
    <!-- Netty 4.1.121.Final: latest stable 4.1.x, supports JDK 25 -->
    <netty.version>4.1.121.Final</netty.version>
    <!-- commons-io 2.18.0: stable, widely used -->
    <commons-io.version>2.18.0</commons-io.version>
    <!-- jakarta.annotation-api 2.1.1: stable, replacement for javax.annotation-api -->
    <jakarta-annotation-api.version>2.1.1</jakarta-annotation-api.version>
  </properties>


  <!-- Central dependency management: import Spring Boot BOM (3.5.3) -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Dependencies not managed by Spring Boot BOM -->
      <dependency>
        <groupId>org.redisson</groupId>
        <artifactId>redisson-spring-boot-starter</artifactId>
        <version>${redisson.version}</version>
      </dependency>
      <dependency>
        <groupId>commons-io</groupId>
        <artifactId>commons-io</artifactId>
        <version>${commons-io.version}</version>
      </dependency>
      <dependency>
        <groupId>jakarta.annotation</groupId>
        <artifactId>jakarta.annotation-api</artifactId>
        <version>${jakarta-annotation-api.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Keep this top-level pom minimal; module-specific deps are declared in modules. -->
  </dependencies>
  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.14.0</version>
          <configuration>
            <source>${java.version}</source>
            <target>${java.version}</target>
            <encoding>${project.build.sourceEncoding}</encoding>
            <annotationProcessorPaths>
              <path>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
              </path>
              <path>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-configuration-processor</artifactId>
                <version>${spring-boot.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.3</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>3.5.3</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>3.5.0</version>
          <configuration>
            <rules>
              <requireJavaVersion>
                <version>[25,25]</version>
              </requireJavaVersion>
            </rules>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <finalName>BackendLab</finalName>
  </build>
</project>
