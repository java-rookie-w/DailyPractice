# ============================================================================
# profile: delay —— 仅当 --spring.profiles.active 带 delay 时加载。
# 延迟 demo 不需要 confirm / manual-ack —— 重点是 TTL 时间线，所以只覆盖存活时长：
# 一条消息最长 10s 延迟，跑 60s 能看到 2 轮完整现象。
# ============================================================================
lab:
  client:
    duration: 60000
