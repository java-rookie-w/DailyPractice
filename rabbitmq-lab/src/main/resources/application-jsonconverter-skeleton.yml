# ============================================================================
# profile: jsonconverter-skeleton —— 练习版骨架（jsonconverter_skeleton 包）专用。
# 与 application-jsonconverter.yml 一致：只覆盖存活时长，队列名在代码常量里带 .sk 后缀。
# ============================================================================
lab:
  client:
    duration: 30000
